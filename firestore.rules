rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For prototype: allow all reads/writes
    // TODO: Lock down for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
